doctype html

<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")

    body
        form(action="upload" method="post" enctype="multipart/form-data")
            input(type="file" name="userfile")
            input(type="submit")
