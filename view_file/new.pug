doctype html
html
    head
        meta(charset="UTF-8")
        title 웹 페이지 만들기
    body
        form(action="/topic", method="post")
            p
                input(type="text" name="title" placeholder="title")
            p
                textarea(name="description")
            p
                input(type="submit")

    